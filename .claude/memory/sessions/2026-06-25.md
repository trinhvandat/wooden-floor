# Session: 2026-06-25

## Done
- Synced master: README PR (#14) had merged; fast-forwarded local master `4fe0c6c..628876b`,
  deleted merged `docs/readme` branch.
- Built **Slice 3 — conversion/desktop polish** on `feat/conversion-polish`:
  - Calculator `~` → "khoảng"; "Xóa bộ lọc" teal → neutral; duplicate CTA disambiguated (sticky
    bar → "Tính chi phí"); product-detail + `/bao-gia` desktop two-column layouts; lead forms →
    inline Zod validation (new `lib/leads/forms.ts`, `components/site/FieldError.tsx`).
  - ProductCard `<img alt>` already correct → dropped from scope.
- Verified: tsc 0, lint 0 (2 pre-existing seed.ts warnings), tests 43/43, build OK (21 routes),
  Playwright at 375 + 1440 (layouts good, inline errors work, 0 console errors).

## Decisions
- [[0013-lead-form-client-zod-validation]] — lightweight client Zod (reuse server field validators),
  no react-hook-form.

## Conventions
- [[playwright-mcp-screenshot-paths]] — MCP screenshots save to MCP cwd; pass absolute path into a
  pre-created dir, clean up with `git clean`.

## Remaining
- Commit + push `feat/conversion-polish` + open PR (memory on-branch).
- Then M4-B (trust/content pages), or cleanup (dead `CtaStrip.tsx`, home meta Zalo mention).

---

# Session (cont.): 2026-06-25 — Projects gallery (M4-B slice 1)

## Done
- Reviewed + posted FUKIONE review on PR #16 (Slice 3) — no issues; user merged; synced master,
  deleted `feat/conversion-polish`.
- Brainstormed → spec'd → planned → built **Projects gallery (M4-B slice 1)** on
  `feat/projects-gallery` via subagent-driven development (6 tasks + 1 gate fix, fresh
  implementer+reviewer per task, opus whole-branch final review = "Ready to merge: YES").
  - `/du-an` grid + `/du-an/[slug]` detail; completed the scaffolded `Projects` collection
    (productId→productIds, images {url,alt}, description, `status` field); gradient fallback;
    seed `status:"published"`; sitemap + breadcrumb JSON-LD; nav repoint + homepage `Xem tất cả`.
  - Design review caught a HIGH gap: detail page had no lead CTA → added `ProjectQuoteCta`
    (opens LeadFormSheet, no context → source "quote") + mobile BottomActionBar; also fixed
    grid missing `<h1>` via `SectionHeading as` prop.
  - Verified: tsc 0, lint 0 (2 pre-existing seed.ts warnings), tests 45/45, build exit 0
    (/projects static + /projects/[slug] SSG), Playwright 375+1440 (0 console errors).

## Decisions
- [[0014-projects-gallery]] — grid+detail completing the scaffolded collection; gradient fallback;
  status/published seed coupling; lead CTA on every content-rich detail page.

## Conventions
- [[leadformsheet-source-by-context]] — LeadFormSheet derives lead `source` from `context`
  presence; pass no context for a plain quote CTA.

## Process note
- A pre-existing `sitemap.test.ts` regressed when Task 4 added `getProjects()` to the sitemap
  (the test mocked only `getProducts`). The plan's self-review missed the existing test and
  Task 4's gates omitted `pnpm test`. Lesson: when a change adds a dependency to a module that
  an existing test mocks, the mock must be extended — run the full suite on any sitemap/data-dep
  change, not just tsc+lint.

## Remaining
- Push `feat/projects-gallery` + open PR (memory on-branch) + `/review-pr`.
- M4-B remaining: about + Google Maps; blog from Articles. Deferred polish: depth:1 test
  assertion, gallery key hardening, desktop grid orphaned-card, 11px chips.
