# Session: 2026-06-25

## Done
- Synced master: README PR (#14) had merged; fast-forwarded local master `4fe0c6c..628876b`,
  deleted merged `docs/readme` branch.
- Built **Slice 3 — conversion/desktop polish** on `feat/conversion-polish`:
  - Calculator `~` → "khoảng"; "Xóa bộ lọc" teal → neutral; duplicate CTA disambiguated (sticky
    bar → "Tính chi phí"); product-detail + `/bao-gia` desktop two-column layouts; lead forms →
    inline Zod validation (new `lib/leads/forms.ts`, `components/site/FieldError.tsx`).
  - ProductCard `<img alt>` already correct → dropped from scope.
- Verified: tsc 0, lint 0 (2 pre-existing seed.ts warnings), tests 43/43, build OK (21 routes),
  Playwright at 375 + 1440 (layouts good, inline errors work, 0 console errors).

## Decisions
- [[0013-lead-form-client-zod-validation]] — lightweight client Zod (reuse server field validators),
  no react-hook-form.

## Conventions
- [[playwright-mcp-screenshot-paths]] — MCP screenshots save to MCP cwd; pass absolute path into a
  pre-created dir, clean up with `git clean`.

## Remaining
- Commit + push `feat/conversion-polish` + open PR (memory on-branch).
- Then M4-B (trust/content pages), or cleanup (dead `CtaStrip.tsx`, home meta Zalo mention).
