# Session: 2026-06-26 — About page + Google Maps (M4-B slice 2)

## Done
- Merged projects gallery (PR #17), synced master, deleted the local+remote branch.
- Brainstormed → spec'd → planned → built **About page (M4-B slice 2)** on `feat/about-maps` via
  subagent-driven development (3 tasks + 1 hardening; fresh implementer+reviewer per task; opus
  whole-branch final review = "Ready to merge: YES").
  - `/gioi-thieu` page: story + trust block + showroom map/contact + lead CTA. Reuses Settings
    NAP/hours/showroom/mapEmbed; LocalBusiness + breadcrumb JSON-LD; nav + sitemap.
  - `extractMapSrc` (`lib/maps.ts`): safe Google-Maps embed src (host allowlist + path check,
    controlled iframe, never dangerouslySetInnerHTML). Adversarial security review found no
    Critical/High; one Minor (`/maps` over-match) hardened to `/maps`-exact-or-`/maps/`.
  - Generalized `ProjectQuoteCta` → shared `LeadCtaSection({ settings, title, body })`.
  - Verified: tsc 0, lint 0 (2 pre-existing seed.ts warnings), tests 53/53, build exit 0
    (/about generates), design-review @375+1440 PASS (0 console errors).

## Decisions
- [[0015-about-page-maps]] — about page at /gioi-thieu; reuse Settings + generalized LeadCtaSection;
  map renders only when `mapEmbed` configured.

## Conventions
- [[safe-embed-iframe-validation]] — operator-supplied embeds → host-allowlist validator → controlled
  `<iframe>`; never `dangerouslySetInnerHTML`; unit-test the bypass vectors.

## Remaining
- Push `feat/about-maps` + open PR (memory on-branch) + `/review-pr`.
- M4-B remaining: blog from Articles (`/tin-tuc`). Deferred polish: footer mobile dup, LeadCtaSection
  button hover, small about copy, section aria-labels, "Trọn gói" card wrap; + carried gallery follow-ups.

---

# Session (cont.): 2026-06-26 — Blog from Articles (M4-B slice 3)

## Done
- Merged about page (PR #18), synced master, deleted local+remote branch.
- Brainstormed → spec'd → planned → built **Blog from Articles (M4-B slice 3)** on `feat/blog-articles`
  via subagent-driven development (5 tasks + 1 polish; fresh implementer+reviewer per task; opus
  whole-branch final review = "Ready to merge: YES"). **M4-B now complete.**
  - `/tin-tuc` list + `/tin-tuc/[slug]` detail; new `lib/data/articles.*`; stub Article type replaced.
  - Lexical `body` rendered via Payload's `RichText` (verified import + typing against the installed
    package .d.ts) — no dangerouslySetInnerHTML, no @tailwindcss/typography dep.
  - `buildArticleJsonLd`, `formatDate`, ArticleCard, nav "Tin tức", sitemap, lead CTA; seeded 3 articles.
  - Verified: tsc 0, lint 0 (2 pre-existing), tests 65/65, build exit 0, live lexical render of real
    <h2>/<p> (0 console errors), design-review @375+1440 PASS.

## Decisions
- [[0016-blog-articles]] — blog at /tin-tuc; articles data module; lexical RichText render; seed samples.

## Conventions
- [[payload-lexical-richtext-render]] — render Payload lexical richText with the official RichText
  React component (`/react`), structural type, Tailwind arbitrary-variant typography, no raw HTML.

## Notes
- A Task 5 reviewer agent stalled (watchdog 600s); re-dispatched and it passed — re-dispatch, don't block.
- The recurring sitemap.test.ts regression (a data-dep added to a module the test mocks) was caught
  PROACTIVELY this time by the implementer running `pnpm test` — the gate-includes-full-suite lesson held.

## Remaining
- Push `feat/blog-articles` + open PR (memory on-branch) + `/review-pr`.
- M5 (next): Zalo widget, GA4, Lighthouse CI, golden e2e flows, go-live. Deferred blog polish:
  LeadCtaSection h2 semantics, ArticleCard title heading, mobile spacing, tag size, sitemap Promise.all.
