# Session — 2026-06-20

## Done
- Designed and built the project memory system (`.claude/memory/` + hooks + `/save-memory` skill)
- Analysis: separate durable vs ephemeral; self-contained system, not wrapped around OMC/ECC
- Seeded decisions/conventions from repo history (mermaid no-emoji, connect actors directly, commit in English)

## Decisions
- Hybrid trigger: Stop hook nudges + skill distills
- Skill placed locally in `.claude/skills/`; ECC left on for A/B testing
- All project artifacts in English; Vietnamese only for user-Claude chat

## Remaining
- End-to-end verify + decide whether to commit `.claude/` to git

## Addendum (end of session)
- All hook-level verification passed (SessionStart inject, Stop fire-once + guard, valid JSON)
- Ran `/save-memory` for real → produced decision 0002 (self-contained memory) + stop-hook-loop-guard convention
- Learned: a Stop hook fires every turn → needs `stop_hook_active` + per-session flag guard to nudge exactly once
- Converted all project artifacts to English per new convention

## Addendum 2 (status review)
- **Memory system COMPLETE & shipped.** Both prior open items closed:
  - `.claude/` committed to git (commit 3fc3fd5) — the "lives with git" goal is met
  - Live-verified: this real new session was auto-injected by the SessionStart hook (not hand-run); re-ran `load-memory.sh` → exit 0, correct output
- Updated HANDOFF.md (goal → complete, Blocked → none) and PLAN.md (ticked remaining boxes, marked COMPLETE)
- No new decisions/conventions to add — all were captured earlier

## Addendum 3 (harness setup)
- Ran `/shipwithai-starter:init` → **Tier 1 harness** for the FUKIONE project.
- Created `CLAUDE.md`, `docs/architecture.md` (SSOT from the design doc), `.claude/starter-context.json`; merged a Node/pnpm `permissions` block into `.claude/settings.json` (memory hooks preserved; both JSON files validated).
- Decisions: Tier 1 (greenfield/docs-only); pnpm; 80% coverage (user overrode design doc's "just enough").
- Gotcha learned: adding `permissions.allow` rules is blocked by the auto-mode classifier until the user explicitly approves the exact rules (a tier confirm isn't enough).
- New memory: decision [[0003-adopt-shipwithai-tier1-harness]], convention [[settings-permission-grant-needs-approval]].
