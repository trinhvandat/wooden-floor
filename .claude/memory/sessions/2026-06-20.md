# Session — 2026-06-20

## Done
- Designed and built the project memory system (`.claude/memory/` + hooks + `/save-memory` skill)
- Analysis: separate durable vs ephemeral; self-contained system, not wrapped around OMC/ECC
- Seeded decisions/conventions from repo history (mermaid no-emoji, connect actors directly, commit in English)

## Decisions
- Hybrid trigger: Stop hook nudges + skill distills
- Skill placed locally in `.claude/skills/`; ECC left on for A/B testing
- All project artifacts in English; Vietnamese only for user-Claude chat

## Remaining
- End-to-end verify + decide whether to commit `.claude/` to git

## Addendum (end of session)
- All hook-level verification passed (SessionStart inject, Stop fire-once + guard, valid JSON)
- Ran `/save-memory` for real → produced decision 0002 (self-contained memory) + stop-hook-loop-guard convention
- Learned: a Stop hook fires every turn → needs `stop_hook_active` + per-session flag guard to nudge exactly once
- Converted all project artifacts to English per new convention
