# Session — 2026-06-22

## Done
- Researched existing design-review agents/skills (local + public GitHub). Found OneRedOak `claude-code-workflows` is the canonical Playwright-MCP design-review workflow; no first-party Anthropic one exists.
- Built a **hybrid** FUKIONE design-review agent: adopted OneRedOak's engine, replaced its S-Tier SaaS-dashboard principles with FUKIONE-specific `design-principles.md` + `style-guide.md` (tokens mirror `globals.css`). Added `/design-review` command + `CLAUDE.md` "Visual development" section.
- Wired a `SubagentStop` cleanup hook so review screenshots auto-delete (tested: 37 artifacts removed, tracked files safe). Gitignored `/*.png` + `.playwright-mcp/`.
- Committed thank-you page fixes surfaced by a live design-review run.
- Opened **PR #2** (`feat/design-review-agent` → `master`) bundling the full Phase 1 UI build + the design-review tooling (treated as one delivery).
- Explained the `remind-save.sh` Stop hook: it is a once-per-session *nudge* to run `/save-memory`, not an auto-saver, guarded against the Stop-hook loop.

## Decisions
- [[0005-adopt-onredoak-design-review]] — hybrid adoption of OneRedOak design-review + SubagentStop auto-cleanup.

## Remaining
- Merge PR #2; delete `/_scratch` before prod.
- Decide whether to commit `.claude/memory/` (currently untracked in git).

---

# Session 2 — scaffold Payload backend (2026-06-22, afternoon)

## Done
- Progress check: PR #2 is **merged** (earlier handoff said "open" — stale); `/_scratch` already gone; project is frontend-only on mock data — backend not built.
- Brainstormed + approved scope: **scaffold-only** Payload backend (no lead wiring / no mock migration), DB = **Neon**.
- Researched Payload 3 ↔ Next 16 from primary sources (doc-specialist agent): Next 16.2.9 IS in `@payloadcms/next` 3.85.1 peerDep range; prod build needs `--webpack` (issue #15429); graphql `^16`.
- Wrote spec `docs/superpowers/specs/2026-06-22-scaffold-payload-backend-design.md` (committed `ad0e65b`).
- **Fixed the hook bug** the user reported (`remind-save.sh: No such file`): relative paths → `$CLAUDE_PROJECT_DIR`, all 4 hooks; verified from a non-root cwd (committed `6c3cf38`). The Stop hook firing this session confirms the fix.
- Scaffolded all files: `payload.config.ts`, 7 collections + Settings global, `(payload)` route group, moved UI into `(app)` via `git mv`, `withPayload` + tsconfig alias, scripts/deps, `.env.example`. (Uncommitted — pending build verify.)
- **Hit Node 25 blocker:** Payload's tsx CLI fails on Node 25 ESM (`?namespace=` resolution). Machine has only brew Node 25, no version manager. User chose **Node 22 LTS**; set `.nvmrc` → 22; user installing via brew.

## Decisions
- [[0006-embed-payload-backend]] — embed Payload, split `(app)`/`(payload)`, 8 collections schema-as-code, scaffold-only.
- [[payload-next16-compat]] — Node 22 not 25, `--webpack` prod build, graphql `^16`, Next 16 supported.
- [[hook-paths-claude-project-dir]] — hooks must use `$CLAUDE_PROJECT_DIR`.

## Remaining (resume — see HANDOFF)
- After Node 22: `pnpm install` → `generate:importmap` + `generate:types` → `pnpm build --webpack` + `pnpm test` → commit + PR.
- Live `/admin` verify needs user's real Neon `.env`.
